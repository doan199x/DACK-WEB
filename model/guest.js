const db = require('../utils/db');

//module.export = {
    //name: async (input) => {
        //const sql = `your sql command` ${nameofobject}
        //const result = await db.load(sql);
        //return result;
    //},
//};